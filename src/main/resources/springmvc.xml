<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:mvc="http://www.springframework.org/schema/mvc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
	   http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.0.xsd
	   http://www.springframework.org/schema/mvc http://www.springframework.org/schema/mvc/spring-mvc-4.0.xsd">

    <context:component-scan base-package="com.poype.springmvc"/>

    <!-- 开启注解校验需要添加这行代码 -->
    <mvc:annotation-driven/>

    <!-- 配置interceptor -->
    <mvc:interceptors>
        <bean class="com.poype.springmvc.interceptor.FirstInterceptor"/>

        <!-- 明确指定SecondInterceptor只作用于 /hello -->
        <mvc:interceptor>
            <mvc:mapping path="/hello"/>
            <bean class="com.poype.springmvc.interceptor.SecondInterceptor"/>
        </mvc:interceptor>
    </mvc:interceptors>
</beans>
